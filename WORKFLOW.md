# 项目工作流程

## 概述

本项目是一个通用域名列表处理系统，用于处理用户上传的任意域名列表文件，并将其转换为 Mihomo 格式的规则文件。

## 项目结构

```
.
├── script/
│   ├── build-combined-rules.sh  # 主构建脚本，检测并处理 txt 目录下所有规则文件
│   └── sort-clash.py            # 处理通用域名列表
├── txt/                        # 用户放置规则文件的目录
│   └── *.txt                   # 用户上传的任意名称的规则文件
├── USAGE.md                    # 使用说明
└── WORKFLOW.md                 # 本工作流程文档
```

## 工作流程

### 1. 用户操作流程

1. **准备规则文件**：用户在 `txt` 目录下创建任意名称的 `.txt` 文件
2. **添加域名**：在文件中每行列出一个域名
3. **运行处理脚本**：执行 `script/build-combined-rules.sh`

### 2. 脚本处理流程

1. **初始化**：下载 Mihomo 工具
2. **文件发现**：扫描 `txt` 目录下所有 `.txt` 文件
3. **批量处理**：对每个检测到的 `.txt` 文件执行以下步骤：
   - 复制源文件到临时处理文件
   - 使用 `sort-clash.py` 处理通用域名列表
   - 对域名进行去重和排序
   - 转换为 Mihomo 文本格式（添加 `+. ` 前缀）
   - 使用 Mihomo 工具转换为 `.mrs` 二进制格式
4. **输出**：生成对应的 `.mrs` 文件到上级目录，同时删除中间的 `.txt` 文件，只保留原始输入文件

### 3. GitHub Actions 工作流程

- 当有文件推送到仓库时，自动触发构建脚本
- 处理 `txt` 目录中所有 `.txt` 文件
- 生成对应的 `.mrs` 文件并提交

## 技术细节

### 文件处理规则

- 所有 `.txt` 文件统一使用 `sort-clash.py` 处理
- 输出对应的 `.mrs` 二进制格式文件
- 保持原有文件名（扩展名除外）

### 排序和去重

- 使用 `sort-clash.py` 对域名进行排序和去重
- 移除子域名冗余（保留父域名）
- 按字母顺序排列

### 格式转换

- 文本格式：每行一个域名
- Mihomo 格式：每行域名前加 `+. ` 前缀
- 二进制格式：使用 Mihomo 工具生成 `.mrs` 文件

## 使用场景

- 个人域名列表管理
- 网络规则集中处理
- 代理软件规则定制
- 防火墙域名规则管理

## 维护说明

- 无需维护上游规则源
- 规则完全由用户自定义
- 支持任意数量的规则文件
- 自动处理文件格式和优化