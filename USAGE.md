# 使用说明

## 概述

此项目已修改为使用手动上传的规则文件，不再从互联网自动下载规则。现在您可以创建或上传任意名称的规则文件到 txt 目录，脚本会自动检测并处理所有 `.txt` 文件。

## 文件结构

```
.
├── script/
│   ├── build-combined-rules.sh  # 构建脚本，处理 txt 目录下所有规则文件
│   └── sort-clash.py            # 处理通用域名列表
├── txt/
│   ├── *.txt                   # 任意名称的规则文件
```

## 如何使用

### 1. 手动创建或编辑规则文件

您可以在 `txt` 目录下创建任意名称的 `.txt` 文件，并添加规则：

- 所有 `.txt` 文件都将使用 `sort-clash.py` 处理，统一作为通用域名列表

### 2. 运行构建脚本

运行以下命令来处理所有规则文件并生成 Mihomo 格式的输出：

```bash
cd script/
bash build-combined-rules.sh
```

这将会：
- 检测 `txt` 目录下所有的 `.txt` 文件
- 对每个文件根据名称选择适当的处理脚本
- 对规则进行排序和去重处理
- 生成 `.mrs` 二进制格式文件到项目根目录
- 删除中间的文本格式文件，只保留原始输入文件

### 3. 规则格式

#### 通用规则格式
- 每行一个域名，如：
```
example.com
subdomain.example.com
```

所有文件都将按照通用域名列表处理

## 工作流程

### 本地工作流程
- 直接运行 `script/build-combined-rules.sh` 处理所有 `.txt` 文件

### GitHub Actions 工作流程
- GitHub Actions 现在只会在推送更改时运行，不再定期自动执行
- 当您向仓库推送对规则文件的更改时，构建脚本会自动运行
- 生成的 `.mrs` 文件会自动更新到主目录

## 注意事项

1. 确保规则文件使用 UTF-8 编码
2. 每行只包含一个域名
3. 不要在规则文件中添加注释行
4. 在提交更改前，请验证规则的正确性

更多详细信息请参见 [WORKFLOW.md](WORKFLOW.md)